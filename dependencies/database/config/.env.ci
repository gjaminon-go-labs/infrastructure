# CI/Test Environment Database Passwords
# This file is committed to git and used ONLY for CI/test environments
# NEVER use these passwords for dev/staging/production!
#
# These passwords are intentionally simple for CI testing

# PostgreSQL admin password (matches GitHub Actions postgres service)
POSTGRES_ADMIN_PASSWORD=postgres

# Billing Service Passwords (test only)
BILLING_MIGRATION_PASSWORD=billing_migration_ci_2025
BILLING_APP_PASSWORD=billing_app_ci_2025

# Catalog Service Passwords (future)
CATALOG_MIGRATION_PASSWORD=catalog_migration_ci_2025
CATALOG_APP_PASSWORD=catalog_app_ci_2025

# Order Service Passwords (future)
ORDER_MIGRATION_PASSWORD=order_migration_ci_2025
ORDER_APP_PASSWORD=order_app_ci_2025