# Database User Passwords Template
# Copy this file to .env.<environment> (e.g., .env.tst, .env.dev, .env.prd)
# and fill in secure passwords for each user
#
# IMPORTANT: Never commit the actual .env files to version control!
# Add .env.* to .gitignore

# PostgreSQL admin password (for database creation)
POSTGRES_ADMIN_PASSWORD=

# Billing Service Passwords
BILLING_MIGRATION_PASSWORD=
BILLING_APP_PASSWORD=

# Catalog Service Passwords (future)
CATALOG_MIGRATION_PASSWORD=
CATALOG_APP_PASSWORD=

# Order Service Passwords (future)
ORDER_MIGRATION_PASSWORD=
ORDER_APP_PASSWORD=

# Password Requirements:
# - Minimum 12 characters
# - Include uppercase, lowercase, numbers, and special characters
# - Different password for each user
# - Store securely (e.g., password manager, vault)